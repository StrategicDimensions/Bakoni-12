<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_invoice_form_inherit_bakoni" model="ir.ui.view">
        <field name="name">account.invoice.form.inherit.bakoni</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_form"/>
        <field name="arch" type="xml">
            <field name="payment_term_id" position="after">
                <field name="tender_ref" required="1"/>
                <field name="customer_ref" required="1"/>
            </field>
        </field>
    </record>
</odoo>